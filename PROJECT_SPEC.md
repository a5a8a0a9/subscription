# SubTrack 專案規格

> 文件狀態：依 2026-07-12 工作區中的程式碼整理。除「後續規劃」外，本文描述目前已實作的行為。

## 1. 產品定位

SubTrack 是供個人使用的繁體中文訂閱管理 PWA。使用者可在不註冊帳號、不連接後端服務的情況下，管理週期性訂閱、查看近期扣款與接收瀏覽器通知。

### 產品目標

- 集中記錄個人的週期性訂閱。
- 快速掌握啟用中的訂閱數、估計月均支出與未來 30 天支出。
- 提供預計扣款月曆及扣款前提醒。
- 安裝為 PWA，並在離線時啟動既有應用程式資源。
- 將資料保留在使用者瀏覽器中，避免建立帳號與後端維運成本。

### 非目標與限制

- 目前不提供登入、多人協作、跨裝置同步或雲端備份。
- 目前不串接銀行、信用卡或第三方訂閱供應商；扣款資料由使用者自行維護。
- 金額目前以新台幣（TWD）顯示，不支援多幣別換算。
- 清除瀏覽器網站資料會移除訂閱資料；重新載入後會以空白清單開始。
- 瀏覽器通知必須由使用者授權，且目前只在應用程式啟動、視窗重新取得焦點或恢復連線時檢查，不保證背景定時推播。

## 2. 技術基線

| 項目      | 規格                                                  |
| --------- | ----------------------------------------------------- |
| Framework | Angular 22，standalone components                     |
| 語言      | TypeScript 6，strict mode                             |
| UI        | Angular Material 22、Bootstrap 5.3、SCSS              |
| 狀態管理  | Angular Signals (`signal`、`computed`)                |
| 表單      | Angular Reactive Forms                                |
| 路由      | Angular Router，頁面元件 lazy loading                 |
| 資料儲存  | Browser `localStorage`，具版本化 payload              |
| 離線能力  | Angular Service Worker、Web App Manifest              |
| 測試      | Jasmine、Karma、ChromeHeadless                        |
| 套件管理  | npm，必須提交 `package-lock.json`                     |
| 語系      | `zh-TW`                                               |
| 支援環境  | 具現代 Web API、`localStorage` 與 ES2022 支援的瀏覽器 |

建議開發環境為 Node.js 22.22.3 與 npm。Production build 輸出至 `dist/sub-track/browser/`。

## 3. 資訊架構與路由

應用程式使用共用 Header、主要內容區及 Footer。未知路由一律導回儀表板。

| 路由          | 頁面     | 用途                             |
| ------------- | -------- | -------------------------------- |
| `/dashboard`  | 儀表板   | 顯示摘要、近期扣款及通知狀態     |
| `/activities` | 訂閱清單 | 瀏覽、搜尋、篩選、排序及維護訂閱 |
| `/calendar`   | 扣款月曆 | 依月份查看預計扣款事件           |
| `/`           | 重新導向 | 導向 `/dashboard`                |

## 4. 功能需求

### 4.1 儀表板

- 顯示啟用中的訂閱總數；暫停及取消項目不納入。
- 顯示估計月均支出：月繳維持原金額、季繳除以 3、年繳除以 12，自訂週期先年化再換算。
- 顯示未來 30 天預計扣款總額與筆數。
- 顯示已進入個別提醒期的近期項目，最多五筆。
- 顯示瀏覽器通知權限狀態，並允許使用者請求權限。

### 4.2 訂閱管理

- 使用者可新增、檢視、編輯及刪除訂閱；刪除前必須二次確認。
- 清單可依名稱或分類文字搜尋。
- 清單可依狀態及分類篩選。
- 清單可依下次扣款日、名稱、金額由高至低或由低至高排序。
- `/activities?create` 可直接開啟新增訂閱對話框。

### 4.3 訂閱資料與驗證

每筆訂閱包含以下欄位：

| 欄位           | 規則                                                 |
| -------------- | ---------------------------------------------------- |
| ID             | 建立時產生 UUID；不支援時以時間戳替代                |
| 名稱           | 必填，去除首尾空白，最多 80 字元                     |
| 金額           | 必填，最小 1，儲存為四捨五入後整數                   |
| 分類           | 必填，可使用影音、音樂、軟體、雲端、學習、健康、其他 |
| 狀態           | `active`、`paused` 或 `cancelled`                    |
| 開始日期       | 必填，`YYYY-MM-DD`                                   |
| 下次扣款日     | 必填，`YYYY-MM-DD`，不得早於開始日期                 |
| 計費週期       | 每月、每季、每年，或每 N 天／月／年                  |
| 提醒           | 可關閉，或設定扣款前 1、3、7、14、30 天              |
| 網站           | 選填，儲存前去除首尾空白                             |
| 備註           | 選填，最多 500 字元，儲存前去除首尾空白              |
| 建立／更新時間 | ISO 8601 timestamp                                   |

### 4.4 扣款日期計算

- 只有 `active` 訂閱會產生扣款事件。
- 以「下次扣款日」作為後續週期的錨點。
- 月、季、年週期遇到目標月份沒有相同日期時，使用該月最後一天。
- 自訂週期支援天、月、年，間隔必須至少為 1。
- 日期以使用者本機日曆日期處理，避免將 `YYYY-MM-DD` 當成 UTC 造成跨日。

### 4.5 月曆與提醒

- 月曆依選定月份列出所有啟用中訂閱的扣款事件。
- 站內提醒依每筆訂閱設定的提前天數計算。
- 瀏覽器通知在取得權限後啟用。
- 同一訂閱、扣款日及檢查日只通知一次；最多保留最近 200 筆通知紀錄。

### 4.6 本機資料

- 訂閱資料鍵值為 `sub-track.activities.v1`，格式為 `{ version: 1, items: Activity[] }`。
- 通知紀錄鍵值為 `sub-track.reminders.v1`。
- 載入時驗證資料版本與最低必要欄位；資料遺失、毀損或版本不支援時，以空白清單重新初始化，不建立範例資料。
- 所有新增、修改及刪除操作都必須立即同步至 `localStorage`。

### 4.7 PWA

- Production build 註冊 Angular Service Worker。
- Service Worker 在應用程式穩定後註冊，最長等待 30 秒。
- Web App Manifest 提供名稱、主題、啟動畫面與多尺寸圖示。
- 應用程式殼層及已建置資源依 `ngsw-config.json` 快取；本機資料仍由 `localStorage` 提供。

## 5. 架構與目錄責任

```text
src/app/
├── layout/                         # 全站版面元件
└── features/activity/
    ├── components/                 # 功能內可重用 UI 與對話框
    ├── data-access/                # Repository、Store、提醒服務
    ├── models/                     # Domain 型別、常數與標籤
    ├── pages/                      # 路由層頁面
    └── utils/                      # 無副作用的日期與計費函式
```

資料流為：頁面／元件觸發操作 → `ActivityStore` 管理狀態與衍生資料 → `ActivityRepository` 讀寫 `localStorage`。日期及費用計算放在純函式中，以便獨立測試。

## 6. 品質與驗收標準

- `npm test -- --watch=false --browsers=ChromeHeadless` 通過。
- `npm run build` 通過 Angular strict template/type checking 與 production budgets。
- initial bundle 超過 500 kB 時警告、超過 1 MB 時失敗。
- 單一元件樣式超過 4 kB 時警告、超過 8 kB 時失敗。
- 新增或修改日期、金額、Store 與持久化邏輯時，必須補上對應單元測試。
- 核心流程需可使用鍵盤操作，表單錯誤與破壞性動作需有清楚回饋。

## 7. 後續規劃（尚未實作）

以下項目不是目前完成範圍，實作前需另行確認：

- 資料匯入、匯出與備份。
- 登入、後端 API、雲端同步與跨裝置使用。
- 多幣別與匯率換算。
- 支出趨勢報表與預算警示。
- 背景推播（Push API／後端排程）。
- Playwright 等端對端測試與 CI pipeline。
